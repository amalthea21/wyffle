# **🧇 Wyffle**

Wyffle is a terminal text editor, most similiar to Nano.

<br><br> 

**Features:**
- ✏️ ANSI Markdown (Front/Background Colors, Formatting..)
- 📁 File Saving/Loading

## **🔨 How to build:**

```
mkdir cmake-build-debug    // CMake directory
cd cmake-build-debug
cmake ..    // To initialize the repository
cmake --build .

// To run it, just:
./wyffle
```
